function Dom() {
    return ( <
        div class = "dom" >
        <
        p >
        <
        u >
        DOM - Document Object Model <
        /u> < /
        p >
        <
        p2 > The Document Object Model DOM is an application programming interface
        for HTML and XML documents.It defines the logical structure of documents and the way a document is accessed and manipulated. < /p2> < /
        div >
    )
}

export default Dom;